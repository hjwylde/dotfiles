# Aliases

# Binaries
alias grep='grep --color=auto'
alias mkdir='mkdir -pv'
alias mv='mv -iv'

# Directories
alias ..='cd ..'
alias ...='cd ../..'
alias la='ls -la'
alias ll='ls -l'
alias l1='ls -1'
